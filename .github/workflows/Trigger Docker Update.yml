name: Trigger Docker update

on:
    schedule:
      # every day at 00:00
      - cron:  '0 0 * * *'

jobs:
    update:
        runs-on: ubuntu-latest
        steps:
        - name: Trigger hook
          run: tlmgr update --reinstall-forcibly-removed --all
